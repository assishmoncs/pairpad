# PairPad Frontend Environment Variables

# Backend Socket.IO URL (default: same origin as web server)
VITE_SOCKET_URL=http://localhost:5000

# Backend REST API URL (default: proxied in dev, absolute URL in prod if hosted on different domain)
VITE_API_URL=http://localhost:5000
